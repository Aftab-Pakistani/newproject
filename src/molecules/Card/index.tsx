import React from "react";
import { Text, View } from "react-native";

type Props = {};

const Card = ({}: Props) => {
  return (
    <View>
      <Text>Card</Text>
    </View>
  );
};

export default Card;
