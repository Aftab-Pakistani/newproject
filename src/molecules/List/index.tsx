import React from "react";
import { Text, View } from "react-native";

type Props = {};

const List = ({}: Props) => {
  return (
    <View>
      <Text>List</Text>
    </View>
  );
};

export default List;
