export enum enum_toast {
  success = "success",
  error = "error",
  default = "default",
}

export enum enum_document_type {
  image = "image",
  video = "video",
  video_url = "video_url",
  audio = "audio",
  pdf = "pdf",
  other = "other",
}
